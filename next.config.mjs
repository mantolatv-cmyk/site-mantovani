/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /* Vercel handles most of the optimization, but you can add custom headers or redirects here */
};

export default nextConfig;
